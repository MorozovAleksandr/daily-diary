import React from 'react';

import Button from './components/primitives/Button';

const App: React.FC = () => {
  return (
    <div className='App'>
      <Button text={'test01'} />
    </div>
  );
};

export default App;
